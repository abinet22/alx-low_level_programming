#include "main.h"

/**
 * _islower - check the code.
 *
 * Return: Always 0.
 */
int _islower('H')
{
    int r;
    int i;
    r = 'H'
    	for (i = 'a'; i <= 'z'; i++)
        {
            if (r == i)
            {
return (1);
            }
        }
	
    
    return (0);
}
